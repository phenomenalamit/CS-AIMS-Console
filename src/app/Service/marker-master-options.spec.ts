import { MarkerMasterOptions } from './marker-master-options';

describe('MarkerMasterOptions', () => {
  it('should create an instance', () => {
    expect(new MarkerMasterOptions()).toBeTruthy();
  });
});
