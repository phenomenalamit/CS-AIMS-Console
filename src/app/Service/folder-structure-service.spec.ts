import { FolderStructureService } from './folder-structure-service';

describe('FolderStructureService', () => {
  it('should create an instance', () => {
    expect(new FolderStructureService()).toBeTruthy();
  });
});
