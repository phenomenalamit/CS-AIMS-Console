import { ModuleListService } from './module-list-service';

describe('ModuleListService', () => {
  it('should create an instance', () => {
    expect(new ModuleListService()).toBeTruthy();
  });
});
