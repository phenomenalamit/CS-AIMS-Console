import { BulkMailAdminstrationService } from './bulk-mail-adminstration-service';

describe('BulkMailAdminstrationService', () => {
  it('should create an instance', () => {
    expect(new BulkMailAdminstrationService()).toBeTruthy();
  });
});
