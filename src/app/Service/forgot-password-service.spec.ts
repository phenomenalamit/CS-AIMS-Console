import { ForgotPasswordService } from './forgot-password-service';

describe('ForgotPasswordService', () => {
  it('should create an instance', () => {
    expect(new ForgotPasswordService()).toBeTruthy();
  });
});
