import { EnvelopeServiceClass } from './envelope-service-class';

describe('EnvelopeServiceClass', () => {
  it('should create an instance', () => {
    expect(new EnvelopeServiceClass()).toBeTruthy();
  });
});
