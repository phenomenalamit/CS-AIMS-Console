import { UserAssignGroupPermissionModel } from './user-assign-group-permission-model';

describe('UserAssignGroupPermissionModel', () => {
  it('should create an instance', () => {
    expect(new UserAssignGroupPermissionModel()).toBeTruthy();
  });
});
