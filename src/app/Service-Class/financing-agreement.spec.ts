import { FinancingAgreement } from './financing-agreement';

describe('FinancingAgreement', () => {
  it('should create an instance', () => {
    expect(new FinancingAgreement()).toBeTruthy();
  });
});
