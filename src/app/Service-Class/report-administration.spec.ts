import { ReportAdministration } from './report-administration';

describe('ReportAdministration', () => {
  it('should create an instance', () => {
    expect(new ReportAdministration()).toBeTruthy();
  });
});
