import { FolderStructureServiceClass } from './folder-structure-service-class';

describe('FolderStructureServiceClass', () => {
  it('should create an instance', () => {
    expect(new FolderStructureServiceClass()).toBeTruthy();
  });
});
