import { ResponsibleOrganization } from './responsible-organization';

describe('ResponsibleOrganization', () => {
  it('should create an instance', () => {
    expect(new ResponsibleOrganization()).toBeTruthy();
  });
});
