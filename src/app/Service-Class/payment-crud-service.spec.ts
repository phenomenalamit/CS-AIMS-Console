import { PaymentCrudService } from './payment-crud-service';

describe('PaymentCrudService', () => {
  it('should create an instance', () => {
    expect(new PaymentCrudService()).toBeTruthy();
  });
});
