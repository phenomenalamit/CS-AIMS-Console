export class ViewColumnAccessServiceClass {
    // userGroup!:string;
    moduleName!:string;
    status!:string;
    columnName: string; // to store label key name
    isEdit: boolean = false; // to store state of fields (shows input text field if true, hides it if false)

}
