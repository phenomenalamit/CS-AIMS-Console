import { OrganizationServiceClass } from './organization-service-class';

describe('OrganizationServiceClass', () => {
  it('should create an instance', () => {
    expect(new OrganizationServiceClass()).toBeTruthy();
  });
});
