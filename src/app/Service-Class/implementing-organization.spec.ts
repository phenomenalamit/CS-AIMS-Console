import { ImplementingOrganization } from './implementing-organization';

describe('ImplementingOrganization', () => {
  it('should create an instance', () => {
    expect(new ImplementingOrganization()).toBeTruthy();
  });
});
